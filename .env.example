# URL del backend — se bakea en el bundle del cliente en build time.
# Local sin Docker:  http://localhost:3001
# Local con Docker:  http://localhost:3001  (puerto 3001 expuesto al host)
# Producción:        https://pay-alert-api.onrender.com  (URL del servicio en Render)
NEXT_PUBLIC_API_URL=http://localhost:3001
